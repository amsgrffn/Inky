<article class="{{post_class}} h-entry">
    <div class="article-body">
        <header>
            <h2 class="p-name"><a class="u-url" href="{{url}}">{{title}}</a></h2>
            <!--{{#if feature_image}}
                <div class="image-box">
                    <picture>
                        <source srcset="
                        {{img_url feature_image size="s" format="avif"}} 280w,
                        {{img_url feature_image size="m" format="avif"}} 480w,
                        {{img_url feature_image size="l" format="avif"}} 1200w,
                        {{img_url feature_image size="xl" format="avif"}} 2000w" sizes="(min-width: 1200px) 480px, 100vw" type="image/avif">
                            <source srcset="
                        {{img_url feature_image size="s" format="webp"}} 280w,
                        {{img_url feature_image size="m" format="webp"}} 480w,
                        {{img_url feature_image size="l" format="webp"}} 1200w,
                        {{img_url feature_image size="xl" format="webp"}} 2000w" sizes="(min-width: 1200px) 480px, 100vw" type="image/webp">
                            <img srcset="
                        {{img_url feature_image size="s"}} 280w,
                        {{img_url feature_image size="m"}} 480w,
                        {{img_url feature_image size="l"}} 1200w,
                        {{img_url feature_image size="xl"}} 2000w" sizes="(min-width: 1200px) 480px, 100vw" src="{{img_url feature_image size="m"}}" class="featured-image" alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}" loading="lazy" width="480" height="480">
                    </picture>
                    {{^if access}}
                        <p class="p-hash-tag">
                            <a href="/#/portal/signup">An Article for Our Beloved Members</a>
                        </p>
                    {{/if}}
                </div>
            {{/if}} -->
        </header>
        <p class="p-summary">
            {{#if custom_excerpt}}
                {{custom_excerpt}}
            {{else}}
                {{excerpt}}
            {{/if}}
        </p>
        <p class="read-more">
            <a href="{{url}}">Read More</a> &hellip;</svg>
        <p>
       <footer class="flex flex-wrap mt-12 items-center">
               <div class="mr-4">
               {{#primary_author}}
                    <a class="avatar" href="{{url}}"><img class="hover:scale-125" src="{{img_url profile_image}}" alt="{{name}}'s Avatar" width="40" height="40" loading="lazy"></a>
                {{/primary_author}}
               </div>
               <div>
                   <p><time class="dt-published" datetime="{{date format="YYYY-MM-DD"}}"><a class="font-bold" href="{{url}}">{{date published_at format="DD MMM YYYY, h:mm A"}}</a></time></p>
                   <p>{{#primary_tag}}<a class="primary-tag" rel="tag" href="{{url}}">{{name}}</a>{{/primary_tag}} &bull; {{tags from="2" limit="1" separator=" &bull; "}}</p>
               </div>
       </footer>
    </div>
</article>